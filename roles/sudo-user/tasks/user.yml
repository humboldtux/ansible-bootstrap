---
- name: Créer l'utilisateur {{ sudo_user }}
  become: true
  ansible.builtin.user:
    name: "{{ sudo_user }}"
    shell: /bin/bash
    groups: sudo
    append: yes
    create_home: yes
    state: present
